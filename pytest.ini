[pytest]
pythonpath = .
addopts = -q -ra
asyncio_mode = auto
testpaths = tests
markers =
    service: mocked async service-level tests
    integration: tests that start real services (e.g., Qdrant via testcontainers)

